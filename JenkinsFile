pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                sh 'javac Jenkins.java'
            }
        }
        stage('Run') {
            steps {
                sh 'java Jenkins'
            }
        }
    }
}
